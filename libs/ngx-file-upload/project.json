{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "ngx-file-upload",
  "sourceRoot": "libs/ngx-file-upload/src",
  "projectType": "library",
  "prefix": "lib",
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": [
        "{workspaceRoot}/dist/{projectRoot}"
      ],
      "options": {
        "project": "libs/ngx-file-upload/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/ngx-file-upload/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/ngx-file-upload/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs ngx-file-upload {args.tag} {args.otp}"
      },
      "dependsOn": [
        "build"
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/ngx-file-upload/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "libs/ngx-file-upload/**/*.ts",
          "libs/ngx-file-upload/**/*.html"
        ]
      }
    }
  }
}